(function() {
    // тут ваш код
})(); // сразу же вызываем функцию
// без скобок вокруг объявления ф-ции JavaScript не поймёт, что мы от него хотим
// так как функция не имеет имени (анонимная), в window она не попадает 